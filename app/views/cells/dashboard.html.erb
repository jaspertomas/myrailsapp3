<p id="notice"><%= notice %></p>

<h1>Dashboard</h1>

<table>
    <% (0..4).each do |i| %>
      <tr>
    <% (0..4).each do |j| %>
        <td><%= @rows[i][j].type_id %></td>
    <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<% @stats.each do |name,setting| %>
<br><%= name %>:<%= setting.value %>:
<% end %>
